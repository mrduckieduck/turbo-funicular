datasources:
  default:
    url: jdbc:postgresql://localhost:5433/funicular
---
micronaut:
  security:
    oauth2:
      clients:
        github:
          client-id: client-id
          client-secret: client-secret